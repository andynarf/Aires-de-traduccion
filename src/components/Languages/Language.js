import React from "react";

import "./Language.css";

const Language = () => {
  return (
    <div className="language">
      <button className="language-active">Eng</button>
      <button>Esp</button>
      <button>Fra</button>
    </div>
  );
};

export default Language;
